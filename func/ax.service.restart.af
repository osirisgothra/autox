ax.service.restart () 
{ 
    complete -A service $FUNCNAME;
    while (($# > 0)); do
        for s in stop start status;
        do
            /usr/bin/sudo service $1 $s;
        done;
        shift;
    done
}
