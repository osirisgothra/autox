python3 () 
{ 
    [[ $# -eq 0 ]] && ?ipython3 || { 
        [[ $COLUMNS -lt 70 && $LINES -lt 12 ]] && { 
            ?ipython3 qtconsole;
            return
        };
        command python3 "$@"
    }
}
